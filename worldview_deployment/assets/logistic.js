"use strict";
const lp={data:"data/worldview-browser-data-v1.0.0.json",codebook:"data/worldview-codebook-v1.0.0.json"};
const ls={data:null,variables:[],binary:[],countries:[],rows:[]};
const E=v=>String(v??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;");
const F=(v,d=3)=>v==null||!Number.isFinite(v)?"Not available":new Intl.NumberFormat("en-NZ",{minimumFractionDigits:d,maximumFractionDigits:d}).format(v);
const logisticSelected=id=>[...document.getElementById(id).selectedOptions].map(o=>o.value);
function logisticParse(o){const n=Object.keys(o),l=o[n[0]].length;if(!n.length||!n.every(k=>o[k].length===l))throw Error("Invalid data");return{columns:o,length:l};}
function logisticTranspose(a){return a[0].map((_,i)=>a.map(r=>r[i]));}
function logisticMultiply(a,b){const bt=logisticTranspose(b);return a.map(r=>bt.map(c=>r.reduce((s,v,i)=>s+v*c[i],0)));}
function logisticInvert(a){const n=a.length,m=a.map((r,i)=>[...r,...Array.from({length:n},(_,j)=>i===j?1:0)]);for(let i=0;i<n;i++){let p=i;for(let r=i+1;r<n;r++)if(Math.abs(m[r][i])>Math.abs(m[p][i]))p=r;if(Math.abs(m[p][i])<1e-10)return null;[m[i],m[p]]=[m[p],m[i]];const q=m[i][i];m[i]=m[i].map(v=>v/q);for(let r=0;r<n;r++){if(r===i)continue;const f=m[r][i];m[r]=m[r].map((v,j)=>v-f*m[i][j]);}}return m.map(r=>r.slice(n));}
function logisticSigmoid(z){if(z>=0){const e=Math.exp(-z);return 1/(1+e);}const e=Math.exp(z);return e/(1+e);}
function logisticFit(outcome,predictors,codes,title){const allow=new Set(codes),cc=ls.data.columns.B_COUNTRY_ALPHA,X=[],y=[];let filtered=0;for(let i=0;i<ls.data.length;i++){if(allow.size&&!allow.has(cc[i]))continue;filtered++;const vals=[ls.data.columns[outcome][i],...predictors.map(p=>ls.data.columns[p][i])];if(vals.some(v=>v===null||v===""))continue;const nums=vals.map(Number);if(nums.every(Number.isFinite)){y.push(nums[0]);X.push([1,...nums.slice(1)]);}}const n=y.length,k=predictors.length+1,events=y.reduce((s,v)=>s+v,0),base={title,filtered,n,excluded:filtered-n,events,nonEvents:n-events};if(n<Math.max(20,k+5))return{...base,status:"insufficient"};if(events===0||events===n)return{...base,status:"one_class"};let beta=Array(k).fill(0),inv=null,converged=false,iterations=0;for(let iter=0;iter<50;iter++){iterations=iter+1;const p=X.map(r=>logisticSigmoid(r.reduce((s,v,j)=>s+v*beta[j],0))),w=p.map(v=>Math.max(v*(1-v),1e-9)),h=Array.from({length:k},()=>Array(k).fill(0)),g=Array(k).fill(0);for(let i=0;i<n;i++){for(let a=0;a<k;a++){g[a]+=X[i][a]*(y[i]-p[i]);for(let b=0;b<k;b++)h[a][b]+=X[i][a]*w[i]*X[i][b];}}inv=logisticInvert(h);if(!inv)return{...base,status:"singular"};const step=logisticMultiply(inv,g.map(v=>[v])).map(r=>r[0]);beta=beta.map((v,j)=>v+step[j]);if(Math.max(...step.map(Math.abs))<1e-8){converged=true;break;}}if(!converged)return{...base,status:"not_converged"};const finalP=X.map(r=>logisticSigmoid(r.reduce((s,v,j)=>s+v*beta[j],0))),finalW=finalP.map(v=>Math.max(v*(1-v),1e-12)),finalH=Array.from({length:k},()=>Array(k).fill(0));for(let i=0;i<n;i++){for(let a=0;a<k;a++){for(let b=0;b<k;b++)finalH[a][b]+=X[i][a]*finalW[i]*X[i][b];}}inv=logisticInvert(finalH);if(!inv)return{...base,status:"singular"};const se=inv.map((r,i)=>Math.sqrt(r[i])),terms=["Intercept",...predictors],coef=beta.map((b,i)=>({term:terms[i],estimate:b,se:se[i],z:b/se[i],oddsRatio:Math.exp(b),orLower:Math.exp(b-1.959963984540054*se[i]),orUpper:Math.exp(b+1.959963984540054*se[i])}));const p=X.map(r=>logisticSigmoid(r.reduce((s,v,j)=>s+v*beta[j],0))),logLik=y.reduce((s,v,i)=>s+v*Math.log(Math.max(p[i],1e-15))+(1-v)*Math.log(Math.max(1-p[i],1e-15)),0),mean=events/n,nullLL=y.reduce((s,v)=>s+v*Math.log(mean)+(1-v)*Math.log(1-mean),0);return{...base,status:"ok",iterations,coef,logLik,aic:-2*logLik+2*k,pseudoR2:1-logLik/nullLL};}
function logisticCard(r,out){if(r.status!=="ok")return`<section class="regression-card"><h3>${E(r.title)}</h3><p class="suppression-note">${r.status==="one_class"?"Only one outcome category is present.":r.status==="singular"?"The model matrix is singular.":r.status==="not_converged"?"The model did not converge.":"Too few complete observations are available."}</p></section>`;const rows=r.coef.map(c=>`<tr><td>${E(c.term)}</td><td>${F(c.estimate)}</td><td>${F(c.se)}</td><td>${F(c.z)}</td><td>${F(c.oddsRatio)}</td><td>${F(c.orLower)} to ${F(c.orUpper)}</td></tr>`).join("");return`<section class="regression-card"><h3>${E(r.title)}</h3><div class="model-fit-grid"><div><strong>${F(r.n,0)}</strong><span>Complete observations</span></div><div><strong>${F(r.events,0)}</strong><span>Events (${E(out.correlationRepresentation.split(";").pop().trim())})</span></div><div><strong>${F(r.aic)}</strong><span>AIC</span></div><div><strong>${F(r.pseudoR2)}</strong><span>McFadden pseudo R squared</span></div></div><div class="table-wrap"><table class="results-table"><thead><tr><th>Term</th><th>Log-odds</th><th>SE</th><th>z</th><th>Odds ratio</th><th>Approx. 95% OR CI</th></tr></thead><tbody>${rows}</tbody></table></div><p class="model-note">The event is coded 1. Odds ratios above 1 indicate higher odds of the event as the predictor increases; values below 1 indicate lower odds.</p><p class="model-note">Rows missing any model variable were excluded (${r.excluded}). Iterations: ${r.iterations}.</p></section>`;}
function calculateLogistic(e){e.preventDefault();const outcome=document.getElementById("logistic-outcome").value,preds=logisticSelected("logistic-predictors");if(!preds.length||preds.length>5||preds.includes(outcome)){document.getElementById("logistic-message").textContent=preds.length>5?"Select no more than five predictors.":preds.includes(outcome)?"The outcome cannot also be a predictor.":"Select at least one predictor.";return;}const countries=logisticSelected("logistic-countries"),map=new Map(ls.countries.map(c=>[c.code,c.name])),sep=document.querySelector("input[name=logistic-country-mode]:checked").value==="separate",specs=sep&&countries.length>1?countries.map(c=>({title:map.get(c)||c,codes:[c]})):[{title:countries.length?countries.map(c=>map.get(c)||c).join(", "):"All countries combined",codes:countries}],results=specs.map(s=>logisticFit(outcome,preds,s.codes,s.title)),out=ls.binary.find(v=>v.id===outcome);document.getElementById("logistic-title").textContent=`${outcome} predicted by ${preds.join(", ")}`;document.getElementById("logistic-message").textContent=`Event coding: ${out.correlationRepresentation}.`;document.getElementById("logistic-results").innerHTML=results.map(r=>logisticCard(r,out)).join("");document.getElementById("logistic-download").hidden=false;ls.rows=results.flatMap(r=>r.status==="ok"?r.coef.map(c=>({country:r.title,outcome,predictors:preds.join(";"),term:c.term,log_odds:c.estimate,se:c.se,z:c.z,odds_ratio:c.oddsRatio,or_ci_lower:c.orLower,or_ci_upper:c.orUpper,n:r.n,events:r.events,aic:r.aic,pseudo_r_squared:r.pseudoR2})):[]);}
function downloadLogistic(){if(!ls.rows.length)return;const cols=Object.keys(ls.rows[0]),q=v=>`"${String(v??"").replaceAll("\"","\"\"")}"`,text=[cols.join(","),...ls.rows.map(r=>cols.map(c=>q(r[c])).join(","))].join("\n"),a=document.createElement("a");a.href=URL.createObjectURL(new Blob([text],{type:"text/csv"}));a.download="worldview-binary-logistic-regression.csv";a.click();URL.revokeObjectURL(a.href);}
function resetLogistic(){document.getElementById("logistic-form").reset();document.getElementById("logistic-outcome").value="Q167";[...document.getElementById("logistic-predictors").options].forEach(o=>o.selected=["Q165","Q260","Q262"].includes(o.value));[...document.getElementById("logistic-countries").options].forEach(o=>o.selected=o.value==="NZL");document.getElementById("logistic-results").innerHTML="";document.getElementById("logistic-download").hidden=true;}
function limitLogisticPredictors(){const s=document.getElementById("logistic-predictors"),x=[...s.selectedOptions];if(x.length>5){x.slice(5).forEach(o=>o.selected=false);document.getElementById("logistic-message").textContent="Select no more than five predictors.";}}
async function initialiseLogistic(){const loading=document.getElementById("logistic-loading");try{const[d,c]=await Promise.all([fetch(lp.data).then(r=>r.json()),fetch(lp.codebook).then(r=>r.json())]);ls.data=logisticParse(d);ls.variables=c.variables.filter(v=>v.correlationEligible);ls.binary=c.variables.filter(v=>v.analysisType==="binary");const pairs=new Map();for(let i=0;i<ls.data.length;i++)pairs.set(ls.data.columns.B_COUNTRY_ALPHA[i],ls.data.columns.B_COUNTRY[i]);ls.countries=[...pairs].map(([code,name])=>({code,name})).sort((a,b)=>a.name.localeCompare(b.name));const o=document.getElementById("logistic-outcome"),p=document.getElementById("logistic-predictors");ls.binary.forEach(v=>o.add(new Option(`${v.id}: ${v.displayName}`,v.id)));ls.variables.forEach(v=>p.add(new Option(`${v.id}: ${v.displayName}`,v.id)));o.value="Q167";[...p.options].forEach(x=>x.selected=["Q165","Q260","Q262"].includes(x.value));const cs=document.getElementById("logistic-countries");ls.countries.forEach(c=>{const x=new Option(`${c.name} (${c.code})`,c.code);if(c.code==="NZL")x.selected=true;cs.add(x);});loading.hidden=true;document.getElementById("logistic-form").hidden=false;}catch(e){loading.textContent=`Logistic regression data could not be loaded: ${e.message}`;}}
document.addEventListener("DOMContentLoaded",()=>{document.getElementById("logistic-form").addEventListener("submit",calculateLogistic);document.getElementById("logistic-reset").addEventListener("click",resetLogistic);document.getElementById("logistic-download").addEventListener("click",downloadLogistic);document.getElementById("logistic-predictors").addEventListener("change",limitLogisticPredictors);initialiseLogistic();});

