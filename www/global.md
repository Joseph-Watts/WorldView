### Global Comparison

This chart is a scatterplot comparing the "average" values for the selected questions for every country in the dataset. The average is a simple calculation based representing the question responses numerically. The ability to hover over a point and identify which country it represents is in development.

The line represents a "best fit" estimate of the relationship between the questions based on a simple linear model. 
 
<br><br><br>
