#### Group-level univariate summary statistics
- Presents descriptive statistics for a single variable aggregated at the group level.
- Values shown correspond to official UNDP group-level indicators, rather than statistics computed from country-level data within the app.