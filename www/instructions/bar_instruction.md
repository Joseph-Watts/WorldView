## Bar Chart

Explore and compare response distributions across countries for specific survey questions.

Only factor variables, both ordered and unordered, can be used as the grouping variable.

Select multiple countries to view side-by-side comparisons of answer frequencies or percentages.

Missing values (NA) are not included in the data used here.